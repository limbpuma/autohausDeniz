---
interface Props {
  class?: string;
  href?: string;
}

const { class: className, href } = Astro.props;
---

<a
  href={href}
  class={`mt-4 inline-block bg-blue-500 text-white py-3 px-10 rounded-full shadow-2xl font-semibold max-w-fit ${className}`}
>
  <slot />
</a>
